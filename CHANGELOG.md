# 更新日志

本文件的条目是开发工作流的一部分：**开始新的 fix / feature 当天**，在文件顶部新增或复用 `## Temp 日期` 版本头（日期为本次开发开始的日期）；**发版时**将 `Temp` 重命名为实际版本号 `vX.Y.Z`，日期保持不变。版本内的功能与修复持续记录在对应小节下。

## Temp 2026-09-14

### 修复

1. CI 的 GitHub Actions 升级至 Node 24 原生版本（`checkout`/`setup-dotnet` v5、`upload-artifact` v6），消除 Node.js 20 弃用警告

## v0.0.6 2026-09-14

### 新增

1. 功能切换快捷键（默认 `Ctrl+Shift+Q`）：在勾选的模式之间循环切换，模式候选为鼠标（操作）、手写、橡皮擦三个复选框（默认全选，至少保留一个，按自左向右顺序循环，当前模式不在候选中时切到最左侧候选）；优先级高于其他快捷键，在弹窗或截图期间其他快捷键被挂起时仍然可用
2. 快捷键支持清空停用：设置中点击输入框右侧的 `×` 按钮或按 `Delete`/`Backspace` 清空（显示灰色“未设置”），对应快捷键不再注册生效
3. 截图预览窗口背景快速切换：新增“保留背景 / 自定义背景”选项，自定义时可选预设色或输入 `#RRGGBB`（默认 `#FFFFFF`），复制与保存按当前所选背景输出；设置中的纯色背景选项仅决定框选阶段与预览的初始背景；预览窗口 UI 与设置弹窗统一（圆角卡片、自绘标题栏、主题化按钮）

### 修复

1. GitHub Actions 发版时自动从 CHANGELOG.md 提取对应版本段落作为 Release 说明（`--notes-file` + `--generate-notes`，对比链接自动追加在正文后）
2. 设置窗口“循环切换”复选框行超出窗口右缘：去掉前缀文本并显式左对齐、收紧间距
3. 截图预览窗口切换背景选项时 UI 偏移：选项行固定高度，展开/收起自定义颜色面板不再引起布局跳动

## v0.0.5 2026-09-14

### 新增

1. 托盘图标与 exe 内嵌应用图标统一（提取失败时回退运行时绘制）
2. CI 自包含单文件开启压缩，体积约 165MB → 78MB

### 修复

1. 框架依赖版发布产物不再打包为 ZIP（内容仅为单个 exe），直接以 `ThinkCanvas-<版本>-win-x64-framework-dependent.exe` 上传

## v0.0.4 2026-09-14

### 新增

1. 退出按钮二次确认弹窗：最小化到系统托盘 / 退出程序 / 不再询问（勾选后可在设置中重新开启提示）
2. 最小化到托盘：隐藏悬浮工具栏并停用全部快捷键，点击托盘图标或托盘菜单恢复
3. 应用图标：exe 内嵌 `app.ico`，托盘图标与之统一（提取失败时回退运行时绘制）
4. Inno Setup 安装包脚本（`packaging/ThinkCanvas.iss`）
5. 图标生成工具（`.tools/icon-gen/`）

### 修复

1. 同时启用 WPF 与 WinForms 时 `Application` 等类型名的全局 using 歧义
2. README 版本号长期滞后（0.0.2 → 0.0.4）

## v0.0.3 2026-09-09

### 新增

1. 开机自动启动（当前用户注册表项，设置页开关）
2. 设置窗口增强与 Overlay / 工具栏交互调整

## v0.0.2 2026-09-09

### 新增

1. CI 打包工作流（`Package`）：手动触发上传 artifact

### 修复

1. 源码按职责重组至 `src/` 各目录，根目录仅保留应用入口与配置

## v0.0.1 2026-09-09

### 新增

1. 初始版本：桌面 Overlay 书写（原生笔输入与压力）、操作 / 书写 / 橡皮擦模式、全局快捷键、悬浮工具栏、区域截图（复制 / 保存 PNG）、设置窗口与持久化、交互式冒烟测试
